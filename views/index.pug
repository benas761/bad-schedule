doctype html
html
    head
        title= pagetitle
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/stylesheets/style.css")
        script(src="/javascripts/schedule.js")
        script.
            //- To prevent XSS attacks b/c of user generated user, schedule and event names
            //- function htmlDecode(input){
            //-     var e = document.createElement('div');
            //-     e.innerHTML = input;
            //-     return e.childNodes.length === 0 ? "" : e.childNodes[0].nodeValue;
            //- }
            window.onload = function(){
                formSchedule(!{dbdata});
            }
    body()
        .header
            p.logo.headertext= pagetitle
            .headerlinkbox
                a(href="https://github.com/benas761/bad-schedule", class="headertext") GitHub
        #schedule.schedule
            p a
        p= dbdata
        